import React from 'react';
import { HomePage } from '@/pages/HomePage';
// You can import global styles here if you have them, e.g.:
// import '@/styles/global.css';

const App: React.FC = () => {
  return (
    <React.StrictMode>
      <HomePage />
    </React.StrictMode>
  );
};

export default App;
