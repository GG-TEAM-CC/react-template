import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';
// If you have global css file in src/styles, import it here
// import '@/styles/global.css'; // Example: if you create one

ReactDOM.createRoot(document.getElementById('root')!).render(
  <App />
);
